{% extends 'base.html.twig' %}

{% block title %}Liste de mes TODOS !{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Liste de mes TODOS! ✅</h1>

    <ul>
    	{% for id,todo in todos %}
    		<li><a href="{{ path('todo_single', {id: id}) }}">
    			{{ todo.title }}
    		</a></li>
    	{% endfor %}
    </ul>


</div>
{% endblock %}
